using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public static class HexColors {
    public static Color INVALID_MOVE = Color.red;
    public static Color VALID_MOVE = Color.cyan;
    public static Color VALID_ATTACK = Color.magenta;
    public static Color VALID_SPELL = Color.green;
    public static Color AVAILABLE_MOVES = new Color(0.87f, 0.87f, 0.78f);
    public static Color CAN_MOVE = Color.green;
    public static Color DEFAULT_COLOR = new Color(0.8f, 0.8f, 0.8f);
}